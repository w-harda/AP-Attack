# AP-Attack 复现实验日志

## 实验约定

- 服务器目标工作树：`/data/ldxvlp/projects/AP-Attack`
- Conda 环境：`apattack`（Python 3.9.25）
- 数据、checkpoint、输出、缓存均应位于 `/data/ldxvlp/...`；不得放入 `/home`。
- 当前阶段：服务器环境与项目级 smoke test 已完成；未下载数据、未启动训练或完整测试。
- 指标约定：记录 mAP、Rank-1/5/10、攻击后 mAP (`mAP_att`) 与 mDR；保存每次运行的完整 stdout/stderr 路径。

## 已完成记录

| 日期 | 主机 | 操作/命令类别 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| 2026-09-07 | 本地 Windows | `git status --short --branch`、`git remote -v` | 当前分支为 `reproduction`，远程与用户提供信息一致。 | 只读。 |
| 2026-09-07 | 本地 Windows | `rg --files`、Python import/路径/API 静态扫描 | 定位两个训练入口、数据/模型/损失/生成器/指标代码，以及 GPU/路径/旧 API 风险。 | 未导入项目模块，避免触发 CLIP 下载或硬编码 checkpoint。 |
| 2026-09-07 | 本地 Windows | 读取 YAML 与关键实现文件 | 形成环境与路径改动候选清单。 | 未执行训练、验证或数据下载。 |
| 2026-09-07 | Ubuntu GPU 服务器 | 环境 smoke test | Python 3.9.25、PyTorch 1.13.1、torchvision 0.14.1、CUDA runtime 11.7、RTX A6000 CUDA 计算、timm 0.6.13、numpy 1.23.5、MKL/intel-openmp 2023.1.0 正常。 | 已完成。 |
| 2026-09-07 | Ubuntu GPU 服务器 | 项目 import smoke test | `config`、`Generator`、`SupConLoss`、`make_model` 均可 import。`model/clip/clip.py` 出现版本 warning，但系字符串比较误判，不影响当前验证。 | 已完成；不修改该 warning。 |
| 2026-09-07 | Ubuntu GPU 服务器 | 依赖兼容性排障 | PyTorch 1.13.1 + MKL 2025.0.0 出现 `iJIT_NotifyEvent`，固定 MKL/intel-openmp 2023.1.0 后解决；NumPy 2.0.1 不兼容，固定 NumPy 1.23.5 后解决。 | 已完成。 |

## 尚未执行的服务器验证（须在批准环境修改方案后逐项填写结果）

| 顺序 | 目的 | 建议命令 | 预期/验收条件 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | 记录环境基线 | `conda activate apattack && python -V && conda list && nvidia-smi` | 保存 Python、包版本、3 张 A6000 可见性。 | 已执行，环境版本已验证。 |
| 2 | 校验框架 | `python -c "import torch, torchvision; print(torch.__version__, torchvision.__version__, torch.version.cuda, torch.cuda.is_available(), torch.cuda.device_count())"` | 输出 `1.13.1`、`0.14.1`、CUDA `11.7`、可见 GPU 数符合启动掩码。 | 已执行，通过。 |
| 3 | 校验核心第三方包 | `python -c "import timm, yacs, ftfy, regex, PIL, tqdm; print(timm.__version__)"` | 无 ImportError，timm 为 `0.6.13`。 | 已执行，通过。 |
| 4 | 校验文件与路径 | `test -d /data/ldxvlp/projects/AP-Attack && test -d /data/ldxvlp/datasets/reid` | 工作树和数据根目录存在；路径写入专用 YAML。 | 未执行 |
| 5 | 校验必要权重 | `find /data/ldxvlp/checkpoints/AP-Attack -maxdepth 2 -type f` | 包含 CLIP、ReID 基线/阶段一、IDE 代理等实际需要的权重。 | 未执行 |
| 6 | 小型 smoke test | `python -c "from config import cfg; from advers.GD import Generator; from loss.supcontrast import SupConLoss; from model.make_model_clipreid import make_model; print('import ok')"` | 核心模块 import 正常；允许 CLIP 版本 warning，不触发下载。 | 已执行，通过。 |
| 7 | 阶段一训练 | `python train_att_model.py --config_file <服务器专用 YAML>` | 按论文/配置完成阶段一，记录 checkpoint 与验证信息。 | 未执行 |
| 8 | 阶段二攻击训练 | `python train_ap_attack.py --config_file <服务器专用 YAML>` | 记录 `best_G_V.pth.tar`、mAP、mAP_att、mDR。 | 未执行 |

## 当前阻塞项

1. 服务器上的数据集、CLIP 缓存、IDE 代理权重和阶段一/目标 checkpoint 的实际文件位置尚未知。
2. 尚未设置 `AP_ATTACK_CLIP_CACHE`；在任何可能触发 CLIP 下载的命令前必须设置到 `/data/ldxvlp/cache/clip`。
3. 原始 README 未提供作者发布权重、数据准备与精确命令；不能在不补充这些信息的情况下声称复现完成。
