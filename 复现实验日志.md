# AP-Attack 复现实验日志

## 实验约定

- 各服务器的路径、硬件与环境状态见 `服务器与运行环境.md`，本日志按 hostname 分别记录。
- 当前阶段：`ubuntu1` 已完成环境与项目级 smoke test；`binarygroup` 已完成基础 Conda、PyTorch/CUDA、第三方依赖与 RTX 4090 CUDA 计算验证，但项目代码尚未部署。
- 两台服务器均未启动训练、完整测试、数据下载或 checkpoint 下载。
- 指标约定：记录 mAP、Rank-1/5/10、攻击后 mAP (`mAP_att`) 与 mDR；保存每次运行的完整 stdout/stderr 路径。

## 已完成记录

| 日期 | 主机 | 操作/命令类别 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| 2026-09-07 | 本地 Windows | `git status --short --branch`、`git remote -v` | 当前分支为 `reproduction`，远程与用户提供信息一致。 | 只读。 |
| 2026-09-07 | 本地 Windows | `rg --files`、Python import/路径/API 静态扫描 | 定位两个训练入口、数据/模型/损失/生成器/指标代码，以及 GPU/路径/旧 API 风险。 | 未导入项目模块，避免触发 CLIP 下载或硬编码 checkpoint。 |
| 2026-09-07 | 本地 Windows | 读取 YAML 与关键实现文件 | 形成环境与路径改动候选清单。 | 未执行训练、验证或数据下载。 |
| 2026-09-07 | `ubuntu1` | 环境 smoke test | Python 3.9.25、PyTorch 1.13.1、torchvision 0.14.1、CUDA runtime 11.7、RTX A6000 CUDA 计算、timm 0.6.13、numpy 1.23.5、MKL/intel-openmp 2023.1.0 正常。 | 已完成。 |
| 2026-09-07 | `ubuntu1` | 项目 import smoke test | `config`、`Generator`、`SupConLoss`、`make_model` 均可 import。`model/clip/clip.py` 出现版本 warning，但系字符串比较误判，不影响当前验证。 | 已完成；不修改该 warning。 |
| 2026-09-07 | `ubuntu1` | 依赖兼容性排障 | PyTorch 1.13.1 + MKL 2025.0.0 出现 `iJIT_NotifyEvent`，固定 MKL/intel-openmp 2023.1.0 后解决；NumPy 2.0.1 不兼容，固定 NumPy 1.23.5 后解决。 | 已完成。 |
| 2026-09-07 | `binarygroup` | 目录规划、系统与基础 Conda 环境检查 | 已记录工作根目录 `/home/lzf/ldx`、Conda 24.9.2、`/usr/local/anaconda3`、base Python 3.12.7，以及独立环境 `/home/lzf/ldx/envs/apattack` 的 Python 3.9.25。 | 已完成；项目代码尚未部署。 |
| 2026-09-07 | `binarygroup` | PyTorch / CUDA 与 RTX 4090 smoke test | PyTorch 1.13.1、torchvision 0.14.1、CUDA runtime 11.7；RTX 4090 compute capability 8.9，`torch.cuda.is_available()` 为 `True`，GPU 数量为 1，CUDA 矩阵运算成功。 | 已通过；非 AP-Attack 项目级验证。 |
| 2026-09-07 | `binarygroup` | 核心第三方依赖 import smoke test | numpy 1.23.5、timm 0.6.13、Pillow 9.5.0、yacs 0.1.8、ftfy 6.1.1、regex 2023.12.25、tqdm 4.66.1、PyYAML 6.0.1、easydict 1.10、matplotlib 3.7.5 均可 import。 | 已通过；项目代码尚未部署，未执行 AP-Attack import。 |

## 尚未执行的服务器验证

| 顺序 | 目的 | 建议命令 | 预期/验收条件 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | `ubuntu1`：记录环境基线 | `conda activate apattack && python -V && conda list && nvidia-smi` | 保存 Python、包版本、3 张 A6000 可见性。 | 已执行，环境版本已验证。 |
| 2 | `ubuntu1`：校验框架 | `python -c "import torch, torchvision; print(torch.__version__, torchvision.__version__, torch.version.cuda, torch.cuda.is_available(), torch.cuda.device_count())"` | 输出 `1.13.1`、`0.14.1`、CUDA `11.7`、可见 GPU 数符合启动掩码。 | 已执行，通过。 |
| 3 | `ubuntu1`：校验核心第三方包 | `python -c "import timm, yacs, ftfy, regex, PIL, tqdm; print(timm.__version__)"` | 无 ImportError，timm 为 `0.6.13`。 | 已执行，通过。 |
| 4 | `ubuntu1`：校验文件与路径 | 根据 `服务器与运行环境.md` 的 A 路径检查项目与数据目录。 | 工作树和数据根目录存在；路径写入专用 YAML。 | 未执行 |
| 5 | `ubuntu1`：校验必要权重 | 根据 `服务器与运行环境.md` 的 A checkpoint 路径检查。 | 包含 CLIP、ReID 基线/阶段一、IDE 代理等实际需要的权重。 | 未执行 |
| 6 | `ubuntu1`：小型 smoke test | `python -c "from config import cfg; from advers.GD import Generator; from loss.supcontrast import SupConLoss; from model.make_model_clipreid import make_model; print('import ok')"` | 核心模块 import 正常；允许 CLIP 版本 warning，不触发下载。 | 已执行，通过。 |
| 7 | 阶段一训练 | `python train_att_model.py --config_file <服务器专用 YAML>` | 按论文/配置完成阶段一，记录 checkpoint 与验证信息。 | 未执行 |
| 8 | 阶段二攻击训练 | `python train_ap_attack.py --config_file <服务器专用 YAML>` | 记录 `best_G_V.pth.tar`、mAP、mAP_att、mDR。 | 未执行 |
| 9 | `binarygroup`：AP-Attack 项目部署与 import smoke test | 项目代码部署后，验证 `config`、`Generator`、`SupConLoss`、`make_model` import。 | 仅在项目目录可用后执行；不得与基础环境 smoke test 混同。 | 未执行 |

## 当前阻塞项

1. `ubuntu1` 的数据集、CLIP 缓存、IDE 代理权重和阶段一/目标 checkpoint 的实际文件位置尚待逐项检查。
2. `binarygroup` 的基础环境已验证，但 AP-Attack 项目代码部署、项目级 import smoke test、数据集和 checkpoint 准备均未完成。
3. 各服务器在任何可能触发 CLIP 下载的命令前，均须设置对应的 `AP_ATTACK_CLIP_CACHE`。
4. 原始 README 未提供作者发布权重、数据准备与精确命令；不能在不补充这些信息的情况下声称复现完成。
