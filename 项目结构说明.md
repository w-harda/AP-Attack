# AP-Attack 项目结构说明

## 审计范围与状态

- 审计日期：2026-09-07。
- 工作树初始状态：`reproduction...origin/reproduction`，无已跟踪文件改动。
- `origin` 指向个人 Fork：`https://github.com/w-harda/AP-Attack.git`；`upstream` 指向作者仓库：`https://github.com/yuanbianGit/AP-Attack.git`。
- 仓库未提供 `requirements.txt`、`environment.yml`、`pyproject.toml`、`setup.py` 或 `.gitignore`。依赖与运行方式只能由源码静态反推。
- 本次仅生成复现文档；未安装依赖、未下载模型/数据、未执行训练或测试，也未修改算法源码。

## 顶层文件

| 路径 | 用途 | 是否为当前 AP-Attack 主流程 |
| --- | --- | --- |
| `train_att_model.py` | 第一阶段训练入口：属性感知 textual inversion（从图像特征生成各属性的连续文本 token）。 | 是 |
| `train_ap_attack.py` | 第二阶段攻击入口：训练通用扰动生成器 `Generator`，并以 mAP/mDR 验证。 | 是 |
| `cmpt_mDR.py` | 从既有日志文本统计 mAP 与 mDR 的离线脚本。含作者机器绝对目录。 | 辅助 |
| `README.md` | 论文题目及引用；没有安装、数据、权重或运行说明。 | 文档 |
| `项目结构说明.md` 等 5 个中文文件 | 本次审计/复现记录。 | 文档 |

## 目录职责

| 目录 | 文件/模块 | 作用 |
| --- | --- | --- |
| `config/` | `defaults.py`、`defaults_base.py` | YACS 配置默认值。`defaults.py` 添加了 `MODEL.ATT`、阶段一/二参数、数据图像/标注目录字段；两个默认文件都仍包含旧 ReID 基线配置。 |
| `configs/att/` | `vit_clipreid_att.yml`、`vit_clipreid_att_PDCL.yml`、`cnn_clipreid_att.yml`、`att_base.yml` | AP-Attack 相关实验配置。默认入口使用 `vit_clipreid_att.yml`。 |
| `configs/person/` | ViT/CNN 基线与 CLIP-ReID 配置 | 人体 ReID 基线配置，非当前攻击入口直接默认配置。 |
| `configs/VehicleID/`、`configs/veri/`、`configs/pedes/` | 车辆、文本行人/VeRi 相关配置 | 保留的扩展实验配置；`pedes` 配置需要并未随仓库提供的 `text_utils` 子模块。 |
| `datasets/` | `make_dataloader_clipreid.py`、数据集类、采样器 | 当前主流程数据工厂、图像读取、Market1501/Duke/MSMT17/Occluded-Duke/VehicleID/VeRi 解析，以及单机/DDP identity sampler。 |
| `model/` | `make_model_clipreid.py`、`clip/` | 当前模型定义。`clip/` 为内置 OpenAI CLIP 实现及 BPE 词表，不依赖外部 `clip` pip 包。 |
| `advers/` | `GD.py`、`spectral.py`、`gumbel.py`、`utils.py` | 扰动生成器、判别器、谱归一化、Gumbel 工具与旧 GAN 辅助代码。当前第二阶段只实例化 `Generator`。 |
| `loss/` | `supcontrast.py`、`make_loss.py`、`triplet_loss.py` 等 | 第一阶段实际使用 `SupConLoss`；传统 ReID 的交叉熵/Triplet/Center/ArcFace 主要保留给旧基线或未走到的代码路径。 |
| `processor/` | `processor_clipreid_att.py`、`processor.py`、`processor_clipreid_stage1.py`、`processor_clipreid_stage2.py` | 第一阶段实际调用 `do_train_att_1`；其他 processor 是旧/替代流程，部分含不安全的导入时副作用。 |
| `solver/` | `make_optimizer_prompt.py`、`scheduler_factory.py`、`cosine_lr.py` 等 | 阶段一选择 `prompt_text_*` 参数的优化器与 cosine scheduler；其余为旧基线优化器/调度器。 |
| `utils/` | `metrics.py`、`reranking.py`、`logger.py` 等 | 日志、mAP/CMC、攻击前后指标、rerank、计量器、I/O 工具。 |
| `misc/` | caption 数据、文本行人数据、构建/评估工具 | 非当前主流程的遗留/扩展代码；直接导入 `misc.eval` 会因缺失 `text_utils.tokenizer` 失败。 |

## 已定位的实际入口与执行链

### 阶段一：属性感知 textual inversion

入口为 `train_att_model.py`。

1. 读取 `configs/att/vit_clipreid_att.yml`（可由 `--config_file` 覆盖）。
2. `datasets/make_dataloader_clipreid.py:make_dataloader` 建立：阶段二 identity-sampler 训练集、阶段一普通 shuffle 训练集、query+gallery 验证集。
3. `model/make_model_clipreid.py:make_model` 在 `MODEL.ATT=True` 时固定返回 `build_transformer_att_LAST`。
4. 该模型以 CLIP visual encoder 的 512 维投影特征为输入，经 5 个 `IM2TEXT` MLP 产生 `upper`、`under`、`hair`、`shoes`、`bag` 的连续 token；`Prompt_Cat_7` 把 token 插入句子模板，`TextEncoder` 得到文本特征。
5. `solver/make_optimizer_prompt.py:make_optimizer_textInverse` 只把名含 `prompt_text_` 的 MLP 参数交给 `AdamW`。
6. `processor/processor_clipreid_att.py:do_train_att_1` 以双向 `SupConLoss` 优化图像/文本特征对应关系，并保存 `ViT-B-16_stage1_<epoch>.pth`。

### 阶段二：通用对抗扰动训练与验证

入口为 `train_ap_attack.py`。

1. 使用同一数据工厂，载入阶段一目标模型 `SOLVER.STAGE2.TARGET_MODEL`。
2. 同时载入 ImageNet ResNet-50 代理模型权重 `./ide_dukemtmcreid.pth`。
3. `advers/GD.py:Generator`（两次下采样、6 个残差块、两次反卷积上采样、谱归一化）生成 RGB 扰动，`L_norm` 将其限制到 8/255。
4. 训练损失是代理 ReID 特征的反向三元组损失，加上 5 个属性 token 的反向三元组损失；配置默认 `ADV_FACTOR=0`、`ADV_FACTOR_3=10`，故实际主要由细粒度属性 token 项驱动。
5. 验证阶段以 IDE 代理网络提取干净/对抗图像特征；`utils/metrics.py:R1_mAP_eval_att` 比较“攻击 query vs 干净 gallery”，输出 mAP、CMC，并以最低 `mAP_att` 保存 `best_G_V.pth.tar`。

## 数据加载与数据集目录约定

当前默认攻击配置的数据集为 `dukemtmc`。`datasets/dukemtmcreid.py` 使用如下实际目录：

```text
<DATASETS.ROOT_DIR>/DukeMTMC-reID/
├── bounding_box_train/
├── query/
└── bounding_box_test/
```

其他工厂键为 `market1501`、`msmt17`、`occ_duke`、`veri`、`VehicleID`。注意 `cuhk_peds` 在工厂中错误映射到 `VehicleID`，并非可直接用于当前主流程。

## 模型、损失与评估定位

| 组件 | 实现位置 | 当前主流程状态 |
| --- | --- | --- |
| CLIP | `model/clip/model.py`、`model/clip/clip.py` | 内置并在首次构建时下载官方 RN50 / ViT-B-16 权重。 |
| 攻击属性模型 | `model/make_model_clipreid.py:build_transformer_att_LAST` | 当前 `make_model` 唯一选择的 ATT 模型。 |
| 文本反演 | `IM2TEXT` + `Prompt_Cat_7` + `TextEncoder` | 5 个属性 token；不是外部 Stable Diffusion textual inversion 实现。 |
| 阶段一损失 | `loss/supcontrast.py:SupConLoss` | 实际使用。 |
| 阶段二损失 | `train_ap_attack.py:adv_TripletLoss` | 实际使用；传统 `make_loss` 被创建但未参与总损失。 |
| 生成器 | `advers/GD.py:Generator` | 实际使用。 |
| 判别器/GANLoss | `advers/GD.py:MS_Discriminator`、`Pat_Discriminator`、`GANLoss` | 被导入/创建 `GANLoss`，但当前入口不训练判别器，也不将 GAN loss 加入总损失。 |
| 攻击指标 | `utils/metrics.py:R1_mAP_eval_att` | 实际使用。 |

## 结构性风险（不在本次修改）

- `MODEL.ATT_TYPE` 在 YAML 中虽有不同取值，但 `make_model` 忽略该字段，始终选择 `build_transformer_att_LAST`；不能据配置名推断实际属性方案。
- `make_model_clipreid.py` 维护了大量 `build_transformer_att_*` / `Prompt_Cat_*` 实验变体，但当前入口不可达。
- `datasets/dukemtmcreid.py` 与 `datasets/occ_duke.py` 会在根目录不存在时自动下载并解压数据。服务器首次运行前必须先放置数据或在源码改动获批准后改为“仅检查不下载”。
- `processor/processor_clipreid_stage2.py` 在模块导入时立即读取作者机器上的生成器 checkpoint；当前两入口没有导入它，但不要将它当成测试入口直接导入。
- 仓库没有 `.gitignore`，未来须在经确认后排除 `/data` 数据、日志、权重和缓存，避免提交大文件。
