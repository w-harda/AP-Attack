# AP-Attack 复现修改日志

## 状态

截至 2026-09-07：**未修改任何算法源码、配置源码或训练参数**。本次只新增审计与复现文档。

| 日期 | 范围 | 修改 | 原因 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-09-07 | 仓库根目录 | 新增 5 份中文复现文档 | 记录静态审计、依赖和后续实验。 | 已完成 |

## 已发现、但尚未获准实施的兼容性改动候选

这些是“跑前阻塞项”，不是本次已实施的改动。应按最小改动原则逐项实施，并且每项都要在本文件补充 diff、命令、日志和对 mAP/mDR 的影响。

| 优先级 | 位置 | 问题 | 建议的最小改动 | 不改的后果 |
| --- | --- | --- | --- | --- |
| P0 | `train_att_model.py:2` | 强制 `CUDA_VISIBLE_DEVICES='1'`，会覆盖作业调度器/用户选择。 | 删除该赋值；由服务器启动命令设置可见卡。 | 单卡或多卡作业可能错误占用物理 GPU 1。 |
| P0 | `model/make_model_clipreid.py` 多处、`train_ap_attack.py`、`advers/GD.py` 等 | 无条件 `.cuda()` / `.to('cuda')`，不尊重 `cfg.MODEL.DEVICE`、local rank 或 CPU。 | 集中定义 `device`，令 tensors/buffers 随模型移动；先只覆盖主入口实际路径。 | 多卡、可见卡重映射、CPU import/test 都不可靠。 |
| P0 | `model/clip/clip.py:_download` | 默认缓存 `/home/<user>/.cache/clip`，与服务器存储约束冲突。 | 默认值改成环境变量（如 `AP_ATTACK_CLIP_CACHE`）或配置中的 `/data/.../cache/clip`。 | 首次 CLIP 权重下载落入 `/home`。 |
| P0 | `train_ap_attack.py:292` | 代理权重写死为 `./ide_dukemtmcreid.pth`，不在 YAML 中。 | 新增一个显式配置键（如 `SOLVER.STAGE2.SURROGATE_MODEL`）并保持默认兼容。 | 无法按服务器 checkpoint 布局部署。 |
| P1 | `configs/att/*.yml` | `ROOT_DIR='../../dataSet'`、输出/权重都是作者相对路径。 | 只在复现实验专用 YAML 中覆盖为 `/data/ldxvlp/...`，不改原作者 YAML。 | 从项目部署目录运行时会找不到数据或写到工作树。 |
| P1 | `datasets/dukemtmcreid.py`、`occ_duke.py` | 数据集缺失时会自动下载/解压。 | 增加默认禁用自动下载或在启动前做明确路径检查。 | 在意外路径/网络环境下触发大下载。 |
| P1 | `processor/processor_clipreid_stage2.py:278` | import 时就读取 `/data4/.../best_G.pth.tar`。 | 将 checkpoint 创建和读取移入函数，并改为配置参数。 | 任何直接 import 都会失败。 |
| P2 | `advers/GD.py` 等 | 旧 API：`F.upsample`、`torch.range`、`.data`、`Variable`。 | 仅在完成 1.13.1 基线复现后单独迁移并做数值回归。 | 不能安全升级至新 PyTorch。 |

## 明确不应做的改动

- 不因服务器驱动显示 CUDA 12.8 而直接升级到 PyTorch 2.x。
- 不重写生成器、损失、属性 token 数量、prompt 模板或数据增强，以免改变论文方法。
- 不把 checkpoint、数据集或 CLIP 缓存放入 Git 工作树、`/home` 或版本控制。
- 不使用 `processor/processor_clipreid_stage2.py` 的硬编码预训练生成器作为当前 `train_ap_attack.py` 的替代路径。
