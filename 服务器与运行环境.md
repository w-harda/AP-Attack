# AP-Attack 服务器与运行环境

本文件记录各服务器的路径、硬件与验证状态。通用依赖 pin、兼容性结论与源码相关说明见 `环境与依赖说明.md`；实验执行历史见 `复现实验日志.md`。两台服务器的信息独立维护，不互相替换。

## 状态总览

| 服务器 | GPU | AP-Attack 环境状态 | 项目 smoke test 状态 |
| --- | --- | --- | --- |
| `ubuntu1`（服务器 A） | NVIDIA RTX A6000 ×3，单卡约 48GB | 已验证 Python 3.9.25、PyTorch 1.13.1 等指定组合。 | 已通过 `config`、`Generator`、`SupConLoss`、`make_model` import 与 CUDA 计算。 |
| `binarygroup`（服务器 B，用户 `lzf`） | NVIDIA GeForce RTX 4090，24GB | 已验证 Python 3.9.25、PyTorch 1.13.1、CUDA runtime 11.7 及核心第三方依赖。 | 项目代码尚未部署，项目级 import smoke test 未执行。 |

## 服务器 A：`ubuntu1`

### 目录与资源

| 项目 | 路径/信息 |
| --- | --- |
| 项目目录 | `/data/ldxvlp/projects/AP-Attack` |
| Conda 环境 | `/data/ldxvlp/miniconda3/envs/apattack` |
| 数据目录 | `/data/ldxvlp/datasets` |
| checkpoint 目录 | `/data/ldxvlp/checkpoints/AP-Attack` |
| 缓存目录 | `/data/ldxvlp/cache` |
| CLIP 缓存环境变量 | `AP_ATTACK_CLIP_CACHE=/data/ldxvlp/cache/clip` |
| GPU | NVIDIA RTX A6000 ×3，单卡约 48GB |

### 已验证的软件与项目状态

- Python 3.9.25
- PyTorch 1.13.1，PyTorch CUDA runtime 11.7
- torchvision 0.14.1、timm 0.6.13、numpy 1.23.5
- MKL 2023.1.0、intel-openmp 2023.1.0（服务器 `conda list` 实测确认）
- RTX A6000 可正常进行 CUDA 计算。
- `config`、`Generator`、`SupConLoss`、`make_model` 可正常 import。

### 运行约束

- GPU 为多人共享资源；启动命令必须显式设置 `CUDA_VISIBLE_DEVICES`，不得依赖源码选择 GPU。
- 访问 `github.com` 会超时。仓库同步、权重或依赖下载应在可联网机器完成后转移，或使用经批准的镜像/内部源。
- 数据、checkpoint 和缓存必须保存在 `/data`，不得放入 `/home`。

## 服务器 B：`binarygroup`（用户 `lzf`）

### 工作根目录与规划

```text
/home/lzf/ldx/
├── envs/
│   └── apattack/             # 已建立：Python 3.9.25
├── projects/
│   └── AP-Attack/            # 规划项目目录，项目代码尚未部署
├── datasets/
├── checkpoints/
│   └── AP-Attack/
├── cache/
│   ├── pip/
│   ├── torch/
│   ├── clip/
│   └── huggingface/
└── outputs/
```

缓存目录规划为：

| 类型 | 路径 |
| --- | --- |
| pip | `/home/lzf/ldx/cache/pip` |
| torch | `/home/lzf/ldx/cache/torch` |
| huggingface | `/home/lzf/ldx/cache/huggingface` |
| CLIP | `/home/lzf/ldx/cache/clip` |

CLIP 缓存环境变量：

```bash
export AP_ATTACK_CLIP_CACHE=/home/lzf/ldx/cache/clip
```

### 已验证的基础环境

- 系统已有 Conda 24.9.2，安装位置为 `/usr/local/anaconda3`。
- base Python 为 3.12.7。
- 独立 Conda 环境为 `/home/lzf/ldx/envs/apattack`，Python 3.9.25。
- PyTorch 1.13.1、torchvision 0.14.1、PyTorch CUDA runtime 11.7。
- GPU 为 NVIDIA GeForce RTX 4090，compute capability 为 8.9。
- `torch.cuda.is_available()` 为 `True`，GPU 数量为 1，CUDA 矩阵运算已成功执行。
- numpy 1.23.5、timm 0.6.13、Pillow 9.5.0、yacs 0.1.8、ftfy 6.1.1、regex 2023.12.25、tqdm 4.66.1、PyYAML 6.0.1、easydict 1.10、matplotlib 3.7.5。
- 核心第三方依赖 import smoke test 已通过。

### 当前状态

| 项目 | 状态 |
| --- | --- |
| 基础 Conda 环境 | 已完成 |
| PyTorch / CUDA | 已验证 |
| 第三方依赖 | 已验证 |
| GPU CUDA 计算 | 已通过 |
| AP-Attack 项目代码部署 | 未完成 |
| AP-Attack 项目级 import smoke test | 未执行 |
| 数据集准备 | 未完成 |
| checkpoint 准备 | 未完成 |
| Stage 1 训练 | 未执行 |
| Stage 2 训练 | 未执行 |

项目代码尚未部署到 `binarygroup`，因此不得将上述基础环境或第三方依赖 smoke test 视为 AP-Attack 项目级 import 验证。未执行训练、测试、数据下载或 checkpoint 下载。
