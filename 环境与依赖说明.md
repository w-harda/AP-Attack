# AP-Attack 环境与依赖说明

## 结论

建议在服务器既有 `apattack` Conda 环境（Python 3.9.25）中使用：

| 项目 | 建议版本 | 选择依据 |
| --- | --- | --- |
| Python | 3.9.25 | 与服务器现状一致；PyTorch 1.13.1 Linux wheel/Conda 包支持 Python 3.9。 |
| PyTorch | 1.13.1 | 对本仓库旧写法兼容较稳：`torch.cuda.amp`、`torch.range`、旧 `torch.utils.data.sampler` 导入与 `Variable` 均仍可运行。 |
| torchvision | 0.14.1 | 与 PyTorch 1.13.1 官方配套；`torchvision.models.resnet50(pretrained=True)` 仍兼容（仅可能告警）。 |
| CUDA runtime | 11.7（首选） | PyTorch 1.13.1 官方提供 CUDA 11.7 包；不要求系统安装完整 CUDA Toolkit。 |
| cuDNN | 由 PyTorch CUDA 包携带 | 不单独 pin 系统 cuDNN，避免与 Conda runtime 混用。 |
| timm | 0.6.13 | 代码直接从 `timm.models.layers` 导入 `DropPath`、`to_2tuple`、`trunc_normal_`；该旧模块路径与 0.6.x 匹配。 |

PyTorch 官方仍列出 `pytorch==1.13.1`、`torchvision==0.14.1` 与 CUDA 11.7 的对应安装组合；亦列出 1.12.1 / 0.13.1 / CUDA 11.6 作为可回退组合：[PyTorch 旧版本官方安装表](https://pytorch.org/get-started/previous-versions/)。服务器驱动为 570.133.07，较 CUDA 11.7 runtime 新；NVIDIA 明确说明新版驱动可向后兼容旧 CUDA runtime，因此 `nvidia-smi` 中的 CUDA 12.8 是驱动支持上限，不是本项目必须使用的 runtime 版本：[NVIDIA CUDA Compatibility](https://docs.nvidia.com/deploy/cuda-compatibility/latest/why-cuda-compatibility.html)。

## 为什么暂不选 PyTorch 2.x / CUDA 12.x

仓库没有锁文件且含多处旧 API：`pretrained=True`、`F.upsample`、`torch.range`、`.data`、`Variable`、`timm.models.layers`。其中一部分在新版本会告警、迁移或已不推荐；直接升级到 PyTorch 2.x / torchvision 新版会把环境复现和源码迁移混为一件事，难以定位数值差异。先使用 1.13.1 + 0.14.1 跑通，是改动最小、可复现实验的起点。

这不是说 CUDA 12.8 不可用，而是它应作为“完成基线复现后的第二套迁移环境”，并单独记录兼容性改动和回归指标。

## 由 import 静态反推的依赖

### 当前两阶段主流程的必需包

| 包 | 建议 pin | 代码证据 |
| --- | --- | --- |
| `torch` | `1.13.1` | 两个入口、模型、生成器、损失、DataLoader。 |
| `torchvision` | `0.14.1` | transforms、ResNet-50、CLIP preprocess。 |
| `timm` | `0.6.13` | `model/make_model_clipreid.py` 的 `timm.models.layers`；数据文件亦导入 `RandomErasing`。 |
| `numpy` | `1.23.5` | 损失、指标、采样、随机数。 |
| `Pillow` | `9.5.0` | `datasets/bases.py` 图像读取及 CLIP 图像处理。 |
| `yacs` | `0.1.8` | `config/defaults*.py`。 |
| `ftfy` | `6.1.1` | 内置 CLIP 的 tokenizer。 |
| `regex` | `2023.12.25` | 内置 CLIP 的 tokenizer。 |
| `tqdm` | `4.66.1` | CLIP 权重下载进度条。 |

### 非当前主流程，但仓库其他模块需要的包

| 包 | 建议 pin | 代码位置/用途 |
| --- | --- | --- |
| `PyYAML` | `6.0.1` | `misc/utils.py`。 |
| `easydict` | `1.10` | `misc/utils.py`。 |
| `matplotlib` | `3.7.5` | `advers/utils.py` 的可视化/保存辅助。 |

未发现当前主流程对外部 `clip`、`scipy`、`sklearn`、`opencv-python`、`pandas` 或 `wandb` 的 import。`torchaudio` 不被源码 import，因此不列为实际依赖。

## CLIP、数据与缓存的服务器落盘策略

内置 `model/clip/clip.py` 的 `_download` 默认使用 `~/.cache/clip`。这违反“不要把大型缓存放进 `/home`”的部署约束。首次运行前应在 shell/作业脚本设置：

```bash
export XDG_CACHE_HOME=/data/ldxvlp/cache
export TORCH_HOME=/data/ldxvlp/cache/torch
```

由于该 CLIP 实现用 `os.path.expanduser("~/.cache/clip")` 而非 `XDG_CACHE_HOME` 或 `TORCH_HOME`，仅设置环境变量**不能**改变其下载目录。需在获准修改源码后，将 `_download` 的默认根目录改为显式配置或从环境变量读取；在此之前，可将 `HOME` 仅对该作业进程设为数据盘下专用目录，或预先在数据盘下载后以软链接提供 `~/.cache/clip`。后两种方案均须先确认服务器策略。

建议统一布局（不在本次创建）：

```text
/data/ldxvlp/
├── projects/AP-Attack/       # Git 工作树
├── datasets/reid/            # 数据集根目录
├── checkpoints/AP-Attack/    # 基线、阶段一、生成器权重
├── outputs/AP-Attack/        # 日志与实验产物
└── cache/clip/               # CLIP 下载缓存
```

## 旧 API 审计结果

| 风险 | 位置 | 1.13.1 方案 | 后续迁移建议 |
| --- | --- | --- | --- |
| `resnet50(pretrained=True)` | `train_ap_attack.py` | 可用，可能出现 deprecation warning。 | 迁移时换为 `weights=ResNet50_Weights...`，需验证预训练权重一致。 |
| `timm.models.layers` | 两个 `make_model*.py` | 0.6.13 可用。 | 新 timm 改为 `timm.layers`。 |
| `F.upsample` | `advers/GD.py` | 可用但弃用。 | 改 `F.interpolate`。 |
| `torch.range` | `advers/GD.py` | 保留兼容性。 | 改 `torch.arange`，同时核查端点语义。 |
| `Variable` / `.data` | `advers/`、`loss/`、攻击脚本 | 可运行，但不安全/弃用风格。 | 以 `torch.no_grad()`、`detach()` 等逐点替换并回归。 |
| `torch.cuda.amp` | 两个阶段训练 | 可用。 | 新版可迁移 `torch.amp.autocast('cuda')`，不应先改。 |

## 安装前的必要检查（本次未执行）

1. 以 `conda list` / `pip freeze` 导出既有 `apattack` 环境，避免无意覆盖已可用版本。
2. 确认 Linux x86_64 与 Conda channels 可访问。
3. 先创建数据盘目录与缓存策略，再安装或下载 CLIP 权重。
4. 安装后仅运行小型 import/版本检查，不运行完整训练。
